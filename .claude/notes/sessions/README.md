# Sessions directory

Per-session terse summaries live here as `YYYY-MM-DD-NN.md` files,
maintained by the `librarian` subagent. Session logs are NOT
auto-loaded into Claude's context — they're queried on demand by the
librarian when the user asks for recall.

To browse, see `../index.md` (top-of-list = most recent).
