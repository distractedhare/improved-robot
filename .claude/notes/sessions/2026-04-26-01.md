# 2026-04-26-01 · Meridian VR QA bugs (5 commits, 8 bugs + deep linking)

## Shipped
- Fixed 7 narrow-viewport + gradient-fallback + badge styling bugs via clamp, @supports, text-gradient utilities.
- Resolved HIGH code-review race condition (DeviceImage useEffect with cached images).
- Fixed QA bug 8: hash-based deep linking in App.tsx (VALID_APP_MODES + lazy useState seed).
- Lowered h1 clamp floor 1.625rem → 1.375rem after local puppeteer catch on 320px wrapping.

## Decisions
- Deep linking: hash-routing only; tab buttons stay as <button> (tab controls, not navigation).
- Verification: static audit + Vercel SSO blocker → pivoted to local puppeteer at 3000.

## Open / next
- Monitor puppeteer selector stability (first run picked wrong h1, needed correction).

## Notes
- clamp() floor tuning: size against LONGEST runtime value, not shortest sample at dev time.
- Vercel SSO password-protection blocked agent-spawn flow; local dev workaround was faster.
