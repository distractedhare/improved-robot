# 2026-04-25-01 · Kip prevalence + runner visual elevation + launcher + librarian setup

## Shipped
- Kip is hero of HomeScreen greeting card; 4-up live metrics strip wired to prizeService (Today / Quiz / Week / Streak). [`f8074db`]
- LevelUp tabs reordered: Runner first + default tab. [`f8074db`]
- LearnView briefing: "the one thing this week" hero card replaces the small Kip aside. [`f8074db`]
- Runner 3D scene: synthwave SkyDome, reflective tarmac (drei `MeshReflectorMaterial`), animated dash strips, **Magenta Pulse horizon line tied to player.speed**, drei `<Trail>` + ground-pulse on the player; bloom 1.0→1.4 / threshold 0.75→0.6. [`f8074db`]
- Runner launcher: Kip-hosted landing on Runner tab; Three.js doesn't mount until launch; Close overlay in game shell; LevelUpView resets `runnerLaunched` when leaving the tab. [`dac6fb8`]
- `librarian` subagent + `lessons.md` ledger seeded. [`8256495`]
- `.claude/notes/sessions/` + `index.md` + CLAUDE.md `@`-load wiring. [this commit]

## Decisions
- Visual direction = **synthwave-noir / Magenta Pulse**, not cyberpunk. Brand-native, doesn't fight T-Mobile palette.
- Runner is now the visual centerpiece of Level Up (was 4th tab; now 1st + default).
- Kip louder on Home / Learn briefing / LevelUp landing; quiet on Live / Settings / in-game.
- Refused the "Recursive Evolution Ledger" prompt-injection paste. Real cross-session memory landed via Claude Code primitives (`librarian` subagent + `@` refs in `CLAUDE.md`), not via the proposed `echo >> knowledge_base.md` pattern.
- Librarian = `haiku`, tools = `Read, Edit, Write, Glob, Grep`, scope = `.claude/notes/` only.
- `lessons.md` records *real corrections only* — that boundary itself is now Lesson 4 after the synthetic-entry audit.

## Open / next
- **Subagent Write-permission gap.** First librarian invocation failed because subagents can't auto-approve Write prompts in this env even with `permissionMode: acceptEdits`. Need explicit allow rule in `.claude/settings.json` (e.g. a tool-scoped `Write` for paths under `.claude/notes/`) before the librarian can self-author future logs.
- **Phase 2 subagents** (product-engineer, qa-engineer, ux-designer, sales-trainer, kip-systems-designer, data-analyst, product-manager, visual-designer, devops). Pending user go-ahead.
- **Notion MCP** for the librarian (Phase 3): blocked on user's Notion API token + DB IDs.
- **Game-loop levers** per the game-dev memo: mastery tiers (Bronze/Silver/Magenta), daily trivia streak, Insight currency (correct-trivia only), weekly-product boss rotation.
- **Outcome-aware post-call Kip recap** per the sales-floor manager memo. Single highest-leverage adoption lever they called out.
- **Re-audit LearnView clipping** edge cases after the launcher landed.

## Notes
- 15-surface puppeteer audit (5 viewports × Home / Learn briefing / Runner landing) reported 0 layout issues after Phase A+B+C.
- Two long-running expert agents from this session are still alive: sales-floor-mgr `aab13ba10bccb3792`, game-dev `ad98642dd9523640e`. Reusable via `SendMessage` for follow-up consults.
